# RapidWright configuration array layouts (T7.5)

See `tests/corpus/xilinx/artix7/rapidwright/README.md` for what these
files are and how they are made.

The `kintexu` directory has no database: its `xcku035.json` is checked
against the part file of prjuray-tools' `ToolsTestData.tar.gz`
(`UltraScale/part.yaml`, the part of its Vivado UltraScale bitstream).
