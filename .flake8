[flake8]
exclude = .git,.tox,build,dist,target
select = E,W,F
# W503 (line break before a binary operator) is deprecated: it contradicts
# W504 (line break after a binary operator), so a codebase can only follow
# one of the two.
ignore = W503
per-file-ignores =
    __init__.py:F401
