[package]
name = "fasm-cli"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
rust-version.workspace = true
description = "Command line tools for FASM: the `fasm` binary (100% command line compatible with the original Python `fasm/tool.py`) and, later, the Xilinx bitstream tool binaries."

[[bin]]
name = "fasm"
path = "src/main.rs"

[dependencies]
fasm = { path = "../fasm" }

[lints]
workspace = true
